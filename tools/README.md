# FloraCore Tools

This directory is reserved for project-wide developer utilities that do not belong specifically to the firmware or FloraOS web application.

Examples include:

- release helpers
- protocol compatibility checks
- development environment checks
- repository maintenance scripts
- test-data generators that contain no production data

Component-specific tooling may remain under `firmware/` or `web/` when it is tightly coupled to that component.
